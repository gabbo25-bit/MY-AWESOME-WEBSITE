name: my_awesome_website
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.0
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  url_launcher: ^6.2.2 # Aggiungi questo per i link esterni
  animated_text_kit: ^4.2.2



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  assets:
    - assets/profile_pic.png
    - assets/project1.png
    - assets/project2.png
    - assets/project3.png